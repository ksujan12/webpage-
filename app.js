function App() {
  return (
    <div className="container">
      <h1>Hello from React!</h1>
      <p>This is a basic webpage with React, JavaScript, HTML, and CSS.</p>
    </div>
  );
}

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
